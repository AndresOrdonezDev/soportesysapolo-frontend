// Copia este archivo como environment.prod.ts y rellena los valores reales.
// environment.prod.ts está en .gitignore y NO se sube al repositorio.
export const environment = {
  production: true,
  apiUrl: 'https://<tu-dominio>/api',
  wsUrl: 'https://<tu-dominio>',
};
